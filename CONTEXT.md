# CONTEXT — ai-sdlc-skills

에이전트가 세션마다 재추론하지 않도록 "왜 이렇게 생겼나"를 고정한 문서. 무엇을/어떻게는 [README](README.md)·[docs/REFERENCE.md](docs/REFERENCE.md) 가 담당한다. 결정을 바꾸려면 이 문서를 먼저 고친다.

## 목적

운영 중인 **기존 저장소**의 버그 수정·기능 요청을 분석 → BA → 영향도 → 명세 → 구현 → QA → 로컬 배포 → 릴리스 준비까지, 각 단계의 근거 문서를 남기며 처리하는 스킬 모음. 산출물은 소비자 저장소의 `.ai-sdlc/runs/<run>/` 에 쌓인다.

## 비목표

- 그린필드 프로젝트 생성기가 아니다. 기존 코드·데이터·운영 경로가 증거의 출발점이다.
- 자동 배포기가 아니다. `release` 는 준비 상태 평가까지이며 운영 배포·프로덕션 데이터 변경은 사용자 명시 승인이 필요하다.
- 특정 그래프/RAG 도구에 의존하지 않는다. 도구가 없으면 Git·소스·스키마·테스트 폴백으로 동작해야 한다.

## 설계 결정과 근거

| 결정 | 근거 | 거부한 대안 |
|---|---|---|
| 단계 전이를 `pipeline_state.py` 가 필수 산출물 존재로 게이트 | 에이전트가 "분석했다"고 말만 하고 넘어가는 것을 막기 위해 파일 존재를 증거로 요구 (#1) | 프롬프트 지시만으로 순서 강제 — 준수율 낮음 |
| 증거는 독립 소스 여러 개를 교차검증(`evidence`), 리뷰 가이드는 스택별 분리 | 한 도구(그래프 한 개)의 출력만 믿으면 blast-radius 누락. 스택마다 리뷰 포인트가 달라 공통 체크리스트는 형식적이 됨 (#4) | 단일 도구 필수화 |
| 커밋/PR 은 상태머신 **밖**의 선택 단계, 절대 자동 push·merge 하지 않음 | 파이프라인 완료와 코드 반영 승인은 다른 결정. 사용자가 명시 요청할 때만 (#4) | implement 끝에 자동 커밋 |
| 이슈 우선 저장소는 `issue` 스킬이 forge 이슈·브랜치·run slug 를 묶음 | 이슈 번호가 브랜치/커밋/PR/run 을 관통해야 추적 가능. 소비자 저장소의 forge 규약(`.claude/rules/forge.md`)에 따름 | 파이프라인 안에서 이슈 생성 여부를 매번 물어봄 |
| 테스트 작성 패스를 구현 패스와 분리(가능하면 별도 서브에이전트) | 같은 컨텍스트가 테스트를 쓰면 구현을 그대로 베낀 테스트가 나옴 → 수용 기준이 아니라 구현을 검증 | 구현 후 같은 세션이 테스트 보강 |
| 컨텍스트 ~40–50% 에서 `handoff` 로 HANDOFF.md 스냅샷 후 새 세션 | 긴 실행은 한 창에 안 들어가고, 절반을 넘기면 앞선 지시가 희석됨. 컴팩션은 무엇이 사라졌는지 보여주지 않지만 명시적 핸드오프 문서는 보여줌 | 자동 compaction 에만 의존 |
| 스킬 이름 접두 `ai-sdlc-skills-` 고정 | 소비자 저장소의 다른 스킬과 네임스페이스 충돌 방지 | 짧은 이름(`analyze` 등) |
| 규칙은 `.claude/rules/` 에 스코프(glob)별로 분리, 항상 로드는 `common.md`·`forge.md` 만 | 항상 로드되는 텍스트는 매 턴 컨텍스트를 먹는다. 파일을 만질 때만 필요한 규칙은 그때만 | 단일 거대 CLAUDE.md |
| `.claude/` 는 동작하는 최소만: rules 3개·pre-commit 훅 1개(배선됨)·memory. 루트 `CLAUDE.md` 는 `@AGENTS.md` 한 줄 | 미배선 훅 6개·유입 메모리·중복 스킬·허구 README 가 쌓여 "설명이 동작보다 많은" 상태였다. dead file 은 감사 비용과 거짓 `[자동강제]` 주장을 낳는다 (#14) | 템플릿 통째 복사 후 방치 |
| `AGENTS.md` 는 자체 완결 — P0/P1 을 본문에 직접 두고 `@` import 등 하네스 전용 문법을 쓰지 않음 | Codex 등 AGENTS.md 호환 에이전트는 `.claude/` 를 로드하지 않는다. 참조 링크에 의존하면 그 하네스에서 절대 규칙에 도달하지 못한다 | `.claude/rules/` 를 가리키는 링크만 두기 |
| 모든 워커는 각자의 git worktree 로 격리, 정식 클론은 default 브랜치 미러로 읽기 전용 | 공유 체크아웃을 쓰던 병렬 세션 두 개가 서로의 작업을 교차오염시킨 사고. 공유 폴더에서 브랜치를 갈아타면 다른 세션의 발밑이 바뀌고, `git add -A` 는 남의 미커밋 작업을 흡수한다. 오케스트레이터(Orca 등)가 worktree 를 소유하면 수동 조작이 도구 상태와 어긋나므로 위임 | 공유 체크아웃 + 브랜치 전환 |

## 불변 제약

- 시크릿·내부 URL 은 저장소와 생성 문서에 기록하지 않는다 (P0, `.claude/rules/common.md`).
- 도구 부재 시 외부 전송·무거운 서비스를 자동 활성화하지 않는다.
- 산출물 경로·상태 이름은 `skills/ai-sdlc-skills-pipeline/references/artifact-contract.md` 와 `pipeline_state.py` 의 `REQUIRED_ARTIFACTS` 가 단일 진실원천.

## 세션 시작 시 읽을 최소 파일

1. 이 문서
2. `docs/REFERENCE.md` — 스킬별 입력/출력 표
3. 작업 대상 스킬의 `skills/<name>/SKILL.md` 하나
4. 재개 중이면 `.ai-sdlc/runs/<run>/HANDOFF.md`

그 외는 필요할 때 탐색한다. 위 4개 이상을 "일단" 읽는 것은 컨텍스트 낭비다.
