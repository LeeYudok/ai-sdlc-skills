# Memory Index

이 프로젝트 메모리 SSOT. 타입접두 `project_`/`feedback_`/`reference_`/`user_`. `user_*` 만 개인(gitignore), 그 외 팀 공유.
메모리 1개 = 파일 1개, 아래에 한 줄 포인터 추가. 코드·git 이력·CONTEXT.md 에서 읽히는 사실은 적지 않는다.

(아직 없음)
